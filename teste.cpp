#include <iostream>
#include <string>

using namespace std;

int main() {
    return 0;
}

/*
//
#include <iostream>
#include <string>
using namespace std;

int convertRomano(string numRoman) {
    int tamanho = numRoman.size();
    int numArab, listInt[tamanho];
    for (int i = 0; i < tamanho; i++) {
        switch (numRoman) {
        case 'I':
            if (numRoman[])
            numArab[i] = 1;
            break;
        case 'V':
            numArab[i] = 5;
            break;
        case 'X':
            numArab[i] = 10;
            break;
        case 'L':
            numArab[i] = 50;
            break;
        case 'C':
            numArab[i] = 100;
            break;
        case 'D':
            numArab[i] = 500;
            break;
        case 'M':
            numArab[i] = 1000;
            break;
        default:
            break;
        }
    }
    // lista.size() -> Retorna bytes ocupados pela variável vetor "lista".
    return numArab;
*/
